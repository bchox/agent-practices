# <project-name> Python guide

Use the configured Python version, environment manager, formatter, and type checker. Keep imports explicit, handle expected errors deliberately, and add tests using the project's existing test framework.

Run `<format-check-command>`, `<typecheck-command>`, and `<test-command>`. Do not commit virtual environments, `.env` files, or generated coverage artifacts.

Task skills live in `.agents/skills/`.
